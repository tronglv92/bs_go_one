apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-one.fullname" . }}
  labels:
    {{- include "go-one.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-one.selectorLabels" . | nindent 4 }}
